@model IEnumerable<nguyenvankhoat591.Models.NVK0591>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NVKName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NVKGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NVKName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NVKGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NVKId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NVKId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NVKId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
